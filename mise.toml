[tools]
bun = "1.2.22"

[env]
# MoonBitツールチェーン（mise未対応のため公式インストーラで管理。バージョンは `mise run moon:setup` が参照する）
MOONBIT_VERSION = "0.1.20260807"
MOON_HOME = "{{env.HOME}}/.moon"
_.path = ["{{env.HOME}}/.moon/bin"]

[tasks."moon:setup"]
description = "MoonBitツールチェーンを公式インストーラで導入（未導入時のみ）"
run = '''
if ! command -v moon >/dev/null 2>&1; then
  curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash -s -- "$MOONBIT_VERSION"
fi
moon version
'''

[tasks."moon:build"]
description = "MoonBit を wasm-gc へビルド"
dir = "moonbit"
run = "moon build --target wasm-gc --release"

[tasks."moon:test"]
description = "MoonBit テスト"
dir = "moonbit"
run = "moon test --target wasm-gc"

[tasks."moon:coverage"]
description = "MoonBit カバレッジレポート"
dir = "moonbit"
run = '''
moon coverage clean
moon test --enable-coverage
moon coverage report -f summary
'''

[tasks.dev]
description = "開発サーバーを起動"
run = "bun run dev"

[tasks.build]
description = "ビルド（MoonBit → wasm-gc → Vite）"
depends = ["moon:build"]
run = "bun run build"

[tasks.test]
description = "TypeScript テスト（vitest）"
run = "bun run test"

[tasks.lint]
description = "oxlint による静的解析"
run = "bun run lint"

[tasks.fmt]
description = "oxfmt によるフォーマット"
run = "bun run fmt"
